prerequisite
-must have a working browser.

installation
-download all the files(zip).
-extract the zip file.
-open files: index.html(to run the photogallery app).

note: challenges files are also included(arrayMap.js and stringTransform)
